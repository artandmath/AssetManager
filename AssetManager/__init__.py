"""A Table to display and edit nukes Nodes."""

# Import built-in modules
import logging

# Although unused this prevents Nuke from crashing when loading with sgtk.
logging.getLogger(__name__).addHandler(logging.NullHandler())
